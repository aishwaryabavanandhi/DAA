Write C programs that demonstrate the mathematical analysis of non-recursive and recursive algorithms.
#include <stdio.h>
unsigned long long factorialRecursive(int n) {
    if (n == 0) {
        return 1;
    }
    return n * factorialRecursive(n - 1);
}

int main() {
    int n;
    printf("Enter a non-negative integer: ");
    scanf("%d", &n);
    if (n < 0) {
        printf("Factorial is not defined for negative numbers.\n");
        return 1; 
    }
    unsigned long long result = factorialRecursive(n);
    printf("Factorial of %d using recursion: %llu\n", n, result);
    return 0;
}
output:
Enter a non-negative integer: 65
Factorial of 65 using recursion: 9223372036854775808
